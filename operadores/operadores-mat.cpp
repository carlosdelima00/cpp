#include <iostream>
#include <math.h>
using namespace std;
int main(){
    int a = 4,b=2;
   /* int soma = a+b,sub = a-b, mult = a * b , div = a/b,pot = pow(a,b);
    cout << "soma: " << soma << "\nsubtracao: " 
    << sub << "\nmultiplicacao: " << mult
     << "\ndivisao: " << div << "\npotencia: " << pot; */
     // incremento e decemento
     cout << "'a' inicial: " << a;
     a++;
     cout << "'a' incrementado: " << a;
     a--;
     a--;
     cout << "'a' decrementado: " << a;

    
    
    

    cout << "\n";
    system("pause");
    return 0;
}